appId: 'rizomalauncher'
productName: 'Rizoma Launcher'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright © 2018-2024 gabirubs'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,.eslintrc.json}'

extraResources:
  - 'libraries'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'gabirubs'
  vendor: 'gabirubs'
  synopsis: 'Modded Minecraft Launcher'
  description: 'Launcher personalizado que permite aos usuários entrar em servidores com mods. Todos os mods, configurações e atualizações são gerenciados automaticamente.'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'